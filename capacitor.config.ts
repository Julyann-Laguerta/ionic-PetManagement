import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.petmanager',
  appName: 'pet-information-manager',
  webDir: 'dist'
};

export default config;
